import { StyleSheet } from 'aphrodite'

const styles = StyleSheet.create({
  registerMargin: {
    marginTop: "25%"
  },

  alreadyUser: {
    float: 'right',
    marginBottom: 10
  },

  hover: {
    ':hover': {
      textDecoration: 'none'
    }
  }
})

export default styles
