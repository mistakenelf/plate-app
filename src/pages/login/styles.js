import { StyleSheet } from 'aphrodite'

const styles = StyleSheet.create({
  loginMargin: {
    marginTop: 100
  },

  nonUser: {
    float: 'right',
    marginBottom: 10
  },

  hover: {
    ':hover': {
      textDecoration: 'none'
    }
  }
})

export default styles
